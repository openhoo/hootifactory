{
  "name": "hootifactory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter '@hootifactory/api' dev",
    "dev:web": "bun run --filter '@hootifactory/web' dev",
    "dev:worker": "bun run --filter '@hootifactory/scan-worker' dev",
    "dev:mail": "bun run --filter '@hootifactory/mail-worker' dev",
    "build": "bun run --filter '@hootifactory/web' build",
    "test": "bun run test:unit",
    "test:unit": "bun run --filter \"@hootifactory/*\" --parallel --elide-lines=0 test:unit",
    "test:integration": "bun run --filter \"@hootifactory/*\" --parallel --elide-lines=0 test:integration",
    "test:all": "bun run test:unit && bun run test:integration",
    "test:e2e": "bun run scripts/playwright.ts test",
    "test:e2e:clients": "bun run scripts/playwright.ts test tests/e2e/*-cli.spec.ts",
    "e2e:install": "bun run scripts/playwright.ts install chromium",
    "check:boundaries": "bun run scripts/check-boundaries.ts",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "format": "biome check --write .",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "bun run packages/db/src/migrate.ts",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:seed": "bun run scripts/seed.ts",
    "compose:up": "docker compose up -d",
    "compose:down": "docker compose down"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.16",
    "@hootifactory/auth": "workspace:*",
    "@hootifactory/config": "workspace:*",
    "@hootifactory/contracts": "workspace:*",
    "@hootifactory/core": "workspace:*",
    "@hootifactory/db": "workspace:*",
    "@hootifactory/queue": "workspace:*",
    "@hootifactory/registry": "workspace:*",
    "@hootifactory/registry-application": "workspace:*",
    "@hootifactory/storage": "workspace:*",
    "@hootifactory/types": "workspace:*",
    "@playwright/test": "^1.60.0",
    "@types/bun": "^1.3.14",
    "drizzle-kit": "^0.31.10",
    "typescript": "^6.0.3"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
